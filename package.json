{
  "name": "shopping-hub",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && if exist public (rmdir /s /q public) && mkdir public && xcopy /s /e /y frontend\\dist\\* public\\",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1",
    "mongodb": "^7.1.0",
    "mongoose": "^9.2.1"
  }
}